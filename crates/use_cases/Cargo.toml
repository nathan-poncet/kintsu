[package]
name = "kintsu-use-cases"
description = "Kintsu's application rules: the interactors and the ports they drive. Depends on the entities only."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
kintsu-entities.workspace = true
