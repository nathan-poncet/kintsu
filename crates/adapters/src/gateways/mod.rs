//! Outbound adapters implementing the ports over the outside world: models,
//! agents, terminals and multiplexers, storage, secrets, the clock. None
//! yet; the first ones come with the daemon and the model layer. The map is
//! in `docs/ARCHITECTURE.md`.
